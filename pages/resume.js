import React from 'react';

import Layout from '../components/Layout';

const Resume = () => {
    return (
        <Layout>
            <div> TEST</div>
        </Layout>
    );
};

export default Resume;
